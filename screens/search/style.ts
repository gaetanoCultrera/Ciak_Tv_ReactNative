import { StyleSheet } from "react-native";
export const { containerSearch } = StyleSheet.create({
  containerSearch: {
    display: "flex",
    alignItems: "center",
    width: "100%",
    height: "100%",
  },
});
