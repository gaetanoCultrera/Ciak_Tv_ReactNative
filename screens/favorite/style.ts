import { StyleSheet } from "react-native";
export const { containerConfetti } = StyleSheet.create({
  containerConfetti: {
    position: "absolute",
    width: "100%",
    height: "100%",
    zIndex: 1,
  },
});
